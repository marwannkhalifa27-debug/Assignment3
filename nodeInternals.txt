1- 
The event loop keeps checking wheather the call stack is empty or not.
When it becomes empty it moves the callbacks to the stack.

2-
Libuv is a libary that enables Node.js to perform non blocking even if it's single threaded.

3-
By using callbacks, promises and async await.
The goal of asynchronous is to not stop the application. 
The function can go on while it keeps reading the rest of the code.

4- 
Call Stack:
The place where javascript executes the functions and are removes when finished executing.

Event Queue:
Is where it keeps the asynchronous functions waiting to be excuted.

Event Loop:
The event loop keeps checking wheather the stack is empty or not.

5-
Libuv provides group of worker threads.
Node.js is single threaded but it uses thread pool for tasks so it doesn't have to wait.

6- Node.js is non blocking despite being single threaded, non blocking lets the program keeps excuting tasks without having to wait.
